# DevTools 开发者工具箱 - 产品设计文档

> 文档版本：v1.3  
> 创建日期：2026-03-20  
> 最后更新：2026-03-23（亮色主题切换）

---

## 一、产品概述

### 1.1 产品定位

DevTools 是一个面向开发者的在线工具集合网站，提供 JSON 处理、时间戳转换等常用开发工具。所有数据处理在浏览器端完成，无需后端服务，确保数据安全。

### 1.2 目标用户

- 程序员、前端/后端开发工程师
- 运维工程师
- 数据分析师
- 其他需要处理 JSON 和时间戳的技术人员

### 1.3 核心价值

| 价值点 | 说明 |
|--------|------|
| 纯前端 | 数据不上传，隐私安全 |
| 快速 | 即开即用，无需注册 |
| 专业 | 面向开发者，功能实用 |
| 可扩展 | 工具注册机制，方便添加新工具 |

---

## 二、技术架构

### 2.1 技术栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Vue | 3.x | Composition API |
| 构建工具 | Vite | 5.x | 快速开发体验 |
| 语言 | TypeScript | 5.x | 类型安全 |
| 路由 | Vue Router | 4.x | 官方路由方案 |
| 状态管理 | Pinia | 2.x | 轻量状态管理（按需引入） |
| 样式 | CSS Variables | - | 暗色主题，无 UI 框架依赖 |
| 样式 | Tailwind CSS + shadcn-vue | - | Tailwind + shadcn-vue styling system |
| 代码高亮 | highlight.js | 11.x | JSON 语法高亮 |

### 2.2 项目结构

```
devtools/
├── public/
│   └── favicon.ico
├── src/
│   ├── assets/
│   │   └── styles/
│   │       └── main.css          # 全局样式
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppHeader.vue     # 顶部导航
│   │   │   ├── AppSidebar.vue    # 工具列表侧边栏
│   │   │   └── AppLayout.vue     # 布局容器
│   │   └── common/
│   │       ├── CodeEditor.vue    # 代码输入/输出组件
│   │       └── CopyButton.vue    # 复制按钮组件
│   ├── pages/
│   │   └── HomePage.vue          # 主页面 - 工具列表
│   ├── tools/
│   │   ├── json-formatter/
│   │   │   └── JsonFormatter.vue # JSON格式化工具页
│   │   └── timestamp-converter/
│   │       └── TimestampConverter.vue # 时间戳转换工具页
│   ├── router/
│   │   └── index.ts              # 路由配置
│   ├── utils/
│   │   ├── json.ts               # JSON处理工具函数
│   │   └── timestamp.ts          # 时间戳处理工具函数
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

### 2.3 工具注册机制

```typescript
// src/tools/registry.ts
interface ToolDefinition {
  name: string           // 工具名称
  path: string           // 路由路径
  icon: string           // 图标
  description: string    // 简介
  category: string       // 分类（用于主页分组显示）
  component: () => Promise<Component>  // 懒加载组件
}

export const tools: ToolDefinition[] = [
  {
    name: 'JSON格式化',
    path: '/json-formatter',
    icon: '{ }',
    description: 'JSON美化、转义',
    category: '数据处理',
    component: () => import('./json-formatter/JsonFormatter.vue')
  },
  {
    name: '时间戳转换',
    path: '/timestamp-converter',
    icon: '⏱',
    description: '时间戳与日期互转',
    category: '时间日期',
    component: () => import('./timestamp-converter/TimestampConverter.vue')
  }
]
```

### 2.4 设计原则

1. **工具独立**：每个工具是一个独立 Vue 组件，有自己的路由和逻辑
2. **工具注册**：通过统一的工具注册机制，方便扩展
3. **响应式**：移动端适配，输入输出区域可堆叠
4. **无依赖**：核心功能不依赖第三方 API，纯本地计算

---

## 三、功能规格

### 3.1 主页面（工具列表）

#### 功能说明

主页面是网站的入口，以方框卡片形式展示所有可用工具，用户点击卡片进入对应工具页面。

#### UI 布局

```
┌─────────────────────────────────────────────────────────────────┐
│  DevTools 开发者工具箱                                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  数据处理                                                        │
│  ┌──────────────────────┐  ┌──────────────────────┐            │
│  │  { }                 │  │  ⏱                   │            │
│  │                      │  │                      │            │
│  │  JSON格式化           │  │  时间戳转换           │            │
│  │  JSON美化、转义       │  │  时间戳与日期互转     │            │
│  │                      │  │                      │            │
│  └──────────────────────┘  └──────────────────────┘            │
│                                                                 │
│  （后续扩展更多分类和工具...）                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 卡片设计规范

```
┌──────────────────────┐
│  { }                 │  ← 图标区（大号图标，居中）
│                      │
│  JSON格式化           │  ← 工具名称（加粗）
│  JSON美化、压缩、     │  ← 工具描述（次要文字色）
│                      │
└──────────────────────┘

卡片尺寸：宽 280px，高 160px
边框：1px solid var(--border-color)
圆角：8px
背景：var(--bg-secondary)
悬停效果：边框变为主题色，轻微上浮阴影
```

#### 交互流程

1. 用户访问首页，看到按分类分组的工具卡片列表
2. 点击任意卡片，跳转到对应工具页面
3. 支持搜索/过滤功能（可选，后续扩展）

#### 路由配置

```typescript
// 首页路由
{
  path: '/',
  name: 'home',
  component: () => import('../pages/HomePage.vue')
}
```

---

### 3.2 JSON 格式化工具

#### 功能列表

| 功能 | 说明 |
|------|------|
| 结构格式化 | 美化 JSON，支持自定义缩进（2/4 空格） |
| 压缩 | 去除所有空白，单行输出 |
| 转义 | 将 JSON 转为字符串转义格式（用于代码中） |
| 消除转义 | 将转义的 JSON 字符串还原 |
| 语法校验 | 检测 JSON 语法错误，显示错误位置 |
| 语法高亮 | 输出区域 JSON 语法着色 |

#### UI 布局

```
┌─────────────────────────────────────────────┐
│  JSON格式化工具                               │
├─────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐   │
│  │                 │  │                 │   │
│  │   输入区域       │  │   输出区域       │   │
│  │   (可编辑)      │  │   (语法高亮)    │   │
│  │                 │  │                 │   │
│  │                 │  │                 │   │
│  └─────────────────┘  └─────────────────┘   │
│                                             │
│  [格式化] [压缩] [转义] [消除转义] [复制] [清空]│
│                                             │
│  缩进: ○2空格 ○4空格  □排序key               │
└─────────────────────────────────────────────┘
```

#### 交互流程

1. 用户在左侧输入区域粘贴/输入 JSON
2. 点击功能按钮（格式化/压缩/转义等）
3. 处理结果在右侧输出区域显示，带语法高亮
4. 如有语法错误，在输入区域下方显示错误信息和位置
5. 点击复制按钮将结果复制到剪贴板

---

### 3.3 时间戳转换工具

#### 功能列表

| 功能 | 说明 |
|------|------|
| 时间戳→日期 | 支持秒/毫秒时间戳转可读日期 |
| 日期→时间戳 | 日期选择器转时间戳 |
| 当前时间戳 | 实时显示当前时间戳（秒/毫秒） |
| 时区转换 | 支持常见时区转换 |
| 批量转换 | 支持多行时间戳批量转换 |

#### UI 布局

```
┌─────────────────────────────────────────────┐
│  时间戳转换工具                               │
├─────────────────────────────────────────────┤
│  当前时间戳: 1710912345 (秒) / 1710912345000 (毫秒) │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │ 时间戳 → 日期                          │  │
│  │ 输入: [________________] [秒/毫秒▼]   │  │
│  │ 结果: 2024-03-20 10:25:45 CST        │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │ 日期 → 时间戳                          │  │
│  │ 选择: [日期时间选择器] [时区▼]         │  │
│  │ 结果: 1710912345 (秒) / 毫秒           │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │ 批量转换                               │  │
│  │ 输入(每行一个):  [多行文本框]           │  │
│  │ 输出:           [多行文本框]           │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

#### 交互流程

1. **当前时间戳**：页面加载后实时更新，每秒刷新
2. **时间戳→日期**：输入时间戳，自动识别秒/毫秒，点击转换显示日期
3. **日期→时间戳**：选择日期时间和时区，自动计算时间戳
4. **批量转换**：每行一个时间戳，批量转换后显示结果

---

## 四、UI 设计规范

### 4.1 主题系统

#### 主题切换
- **位置**：Header 右侧，版本号 Badge 左侧
- **交互**：点击按钮切换亮色/暗色模式
- **图标**：亮色模式显示月亮图标，暗色模式显示太阳图标
- **持久化**：用户偏好存储在 localStorage

#### 亮色主题配色
```css
/* shadcn-vue 亮色主题 */
:root {
  --color-background: hsl(0 0% 100%);       /* #ffffff */
  --color-foreground: hsl(0 0% 9%);         /* #171717 */
  --color-card: hsl(0 0% 100%);
  --color-card-foreground: hsl(0 0% 9%);
  --color-primary: hsl(0 0% 9%);
  --color-primary-foreground: hsl(0 0% 98%);
  --color-secondary: hsl(0 0% 96%);
  --color-secondary-foreground: hsl(0 0% 9%);
  --color-muted: hsl(0 0% 96%);
  --color-muted-foreground: hsl(0 0% 45%);
  --color-border: hsl(0 0% 90%);
  --color-input: hsl(0 0% 90%);
  --color-ring: hsl(0 0% 15%);
}
```

#### 暗色主题配色
*设计系统现在使用 shadcn-vue CSS 变量体系*

```css
/* shadcn-vue 暗色主题 */
.dark {
  --color-background: hsl(0 0% 5%);         /* #0d0d0d */
  --color-foreground: hsl(0 0% 91%);        /* #e8e8e8 */
  --color-card: hsl(0 0% 12%);
  --color-card-foreground: hsl(0 0% 91%);
  --color-primary: hsl(0 0% 98%);
  --color-primary-foreground: hsl(0 0% 9%);
  --color-secondary: hsl(0 0% 14%);
  --color-secondary-foreground: hsl(0 0% 91%);
  --color-muted: hsl(0 0% 18%);
  --color-muted-foreground: hsl(0 0% 60%);
  --color-border: hsl(0 0% 14.9%);
  --color-input: hsl(0 0% 14.9%);
  --color-ring: hsl(0 0% 83%);
}
```

### 4.2 组件规范

| 组件 | 样式 |
|------|------|
| 按钮 | 使用 shadcn-vue Button，variant: default/secondary/outline/ghost |
| 输入框 | 使用 Tailwind 类名，border + bg-background |
| 代码区域 | 等宽字体，行号，语法高亮 |
| 卡片 | 使用 shadcn-vue Card，bg-card + border-border |

### 4.3 响应式设计

| 断点 | 布局 |
|------|------|
| > 768px | 输入输出并排显示 |
| ≤ 768px | 输入输出堆叠显示 |

---

## 五、开发计划

### 阶段一：基础搭建（1-2 天）

- [ ] Vite + Vue 3 + TypeScript 项目初始化
- [ ] 暗色主题基础样式
- [ ] 路由配置和工具注册机制
- [ ] 基础布局组件（Header、Sidebar、Layout）
- [ ] 主页面开发（工具卡片列表）

### 阶段二：JSON 格式化工具（2-3 天）

- [ ] CodeEditor 组件开发
- [ ] JSON 解析和格式化逻辑
- [ ] 转义/消除转义功能
- [ ] 语法高亮
- [ ] 复制、清空功能
- [ ] 错误提示

### 阶段三：时间戳转换工具（1-2 天）

- [ ] 当前时间戳实时显示
- [ ] 时间戳→日期转换
- [ ] 日期→时间戳转换
- [ ] 时区选择
- [ ] 批量转换

### 阶段四：优化和部署（1 天）

- [ ] 移动端适配
- [ ] 性能优化
- [ ] 部署到 Vercel / GitHub Pages

---

## 六、部署方案

### 6.1 推荐平台

| 平台 | 优势 | 说明 |
|------|------|------|
| Vercel | 全球 CDN，自动部署 | 连接 Git 仓库后自动构建部署 |
| GitHub Pages | 免费，简单 | 适合个人项目 |
| Netlify | 功能丰富 | 支持表单、函数等 |

**推荐 Vercel**：免费额度充足，全球 CDN 加速，支持自动部署。

### 6.2 部署流程

1. 将代码推送到 GitHub 仓库
2. 在 Vercel 导入项目
3. 配置构建命令：`npm run build`
4. 配置输出目录：`dist`
5. 自动部署完成

---

## 七、扩展规划

### 7.1 后续工具建议

| 工具 | 说明 |
|------|------|
| Base64 编解码 | Base64 字符串编码/解码 |
| URL 编解码 | URL 编码/解码 |
| 正则表达式测试 | 在线正则匹配测试 |
| 代码格式化 | JSON/JS/CSS 代码格式化 |
| Diff 对比 | 文本差异对比 |
| 颜色转换 | HEX/RGB/HSL 颜色转换 |
| UUID 生成 | 在线生成 UUID |
| Hash 计算 | MD5/SHA1/SHA256 计算 |

### 7.2 扩展方式

通过工具注册机制，添加新工具只需：

1. 在 `src/tools/` 下创建新工具目录
2. 开发工具组件
3. 在 `registry.ts` 中注册工具（包含 category 字段）
4. 自动出现在侧边栏、主页卡片列表和路由中

**主页自动更新**：主页通过读取 `registry.ts` 中的工具列表，自动生成分类分组的卡片展示，无需手动维护主页内容。

---

## 八、风险与挑战

| 风险 | 应对措施 |
|------|----------|
| 大 JSON 处理性能 | 限制输入大小，添加处理进度提示 |
| 浏览器兼容性 | 使用现代 API，优雅降级 |
| 移动端体验 | 响应式设计，简化移动端功能 |

---

## 九、总结

DevTools 开发者工具箱是一个面向开发者的在线工具集合，首期包含 JSON 格式化和时间戳转换两个核心工具。采用 Vite + Vue 3 + TypeScript 技术栈，纯前端实现，暗色主题，响应式设计。通过工具注册机制，方便后续扩展更多工具。

---

*文档结束*
